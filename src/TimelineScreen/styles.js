import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    name: {
        fontSize: 20,
    },
    username: {
        fontSize: 15,
        color: 'gray'
    },
});